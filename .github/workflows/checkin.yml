name: Desk Check-in Cron

on:
  schedule:
    # Run at 8:48 AM EST (9:48 AM EDT during daylight saving)
    # 12 minutes before typical 9:00 AM reservation start time
    - cron: "48 13 * * 1-5"
  workflow_dispatch:
    inputs:
      user:
        description: 'User to check in (enter their email address or "all")'
        required: false
        default: 'all'
        type: string

jobs:
  set-users:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      users: ${{ steps.set-users.outputs.users }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install jq
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends jq

      - name: Fetch users from Google Sheet
        env:
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
        run: |
          chmod +x fetch_users.sh
          ./fetch_users.sh ./USER_CONFIGS.json

      - name: Parse users
        id: set-users
        run: |
          USER_CONFIGS=$(cat ./USER_CONFIGS.json)

          ALL_USERS=$(echo "$USER_CONFIGS" | jq -r 'keys[]' | jq -R -s -c 'split("\n")[:-1]')

          if [ "${{ github.event_name }}" == "workflow_dispatch" ] && [ -n "${{ github.event.inputs.user }}" ] && [ "${{ github.event.inputs.user }}" != "all" ]; then
            USER="${{ github.event.inputs.user }}"
            if echo "$USER_CONFIGS" | jq -e ".\"$USER\"" > /dev/null 2>&1; then
              echo "users=[\"$USER\"]" >> $GITHUB_OUTPUT
            else
              echo "Error: User '$USER' not found in USER_CONFIGS"
              exit 1
            fi
          else
            echo "users=$ALL_USERS" >> $GITHUB_OUTPUT
          fi

  run-checkin:
    needs: set-users
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      matrix:
        user: ${{ fromJSON(needs.set-users.outputs.users) }}
      fail-fast: false
      max-parallel: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install jq
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends jq

      - name: Run check-in for ${{ matrix.user }}
        env:
          APPSPACE_HOST: ${{ secrets.APPSPACE_HOST }}
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
          RESERVATION_USER: ${{ matrix.user }}
        continue-on-error: true
        run: |
          chmod +x checkin.sh fetch_users.sh
          ./checkin.sh || {
            echo "::error::Check-in failed for user ${{ matrix.user }}"
            exit 1
          }

      - name: Report failure
        if: failure()
        run: |
          echo "::warning::Check-in job failed for user ${{ matrix.user }}"
          echo "Check logs above for details"
