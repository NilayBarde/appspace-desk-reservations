// Shown in the overlay's top bar so users can tell which release they're on.
// Bump on every change to docs/app (CI enforces this): patch for fixes,
// minor for features.
export const VERSION = "1.0.0";
